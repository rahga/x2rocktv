<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Everything goes to the Sonos cloud Control API over HTTPS. When local player control lands
      (see ARCHITECTURE.md) this is where the .local cleartext exemption for mDNS-discovered
      speakers goes back in; until then there is nothing to exempt.
    -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
